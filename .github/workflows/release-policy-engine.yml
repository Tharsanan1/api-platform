name: Release Policy Engine

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., v1.0.0-m4)'
        required: true
        type: string

env:
  IMAGE_NAME: api-platform-policy-engine
  REGISTRY: ghcr.io
  REGISTRY_USERNAME: tharsanan1
  REPO_NAME: api-platform

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Update VERSION file
        working-directory: gateway/policy-engine
        run: |
          echo "${{ inputs.version }}" > VERSION
          echo "Updated VERSION file:"
          cat VERSION

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ env.REGISTRY_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build Docker image
        working-directory: gateway/policy-engine
        run: |
          docker build -f Dockerfile.runtime -t wso2/${{ env.IMAGE_NAME }}:${{ inputs.version }} ../

      - name: Tag Docker image
        run: |
          docker tag wso2/${{ env.IMAGE_NAME }}:${{ inputs.version }} ${{ env.REGISTRY }}/${{ env.REGISTRY_USERNAME }}/${{ env.REPO_NAME }}/${{ env.IMAGE_NAME }}:${{ inputs.version }}

      - name: Push Docker image
        run: |
          docker push ${{ env.REGISTRY }}/${{ env.REGISTRY_USERNAME }}/${{ env.REPO_NAME }}/${{ env.IMAGE_NAME }}:${{ inputs.version }}

      - name: Commit version changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add gateway/policy-engine/VERSION
          git commit -m "chore: bump policy-engine version to ${{ inputs.version }}"

      - name: Create and push Git tag
        run: |
          git tag -a "policy-engine-${{ inputs.version }}" -m "Release Policy Engine ${{ inputs.version }}"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git "policy-engine-${{ inputs.version }}"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main

      - name: Image Summary
        run: |
          echo "### Policy Engine Image Released :rocket:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** \`${{ inputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Git Tag:** \`policy-engine-${{ inputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** \`${{ env.REGISTRY }}/${{ env.REGISTRY_USERNAME }}/${{ env.REPO_NAME }}/${{ env.IMAGE_NAME }}:${{ inputs.version }}\`" >> $GITHUB_STEP_SUMMARY
