events {}

http {
  default_type application/json;

  server {
    listen 8443 ssl;
    server_name mtls-backend-wronghost;

    ssl_certificate     /etc/mtls-pki/backend-server-wronghost.crt;
    ssl_certificate_key /etc/mtls-pki/backend-server-wronghost.key;

    ssl_client_certificate /etc/mtls-pki/ca-a.crt;
    ssl_verify_client on;
    ssl_verify_depth 3;

    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
      add_header X-Client-Subject $ssl_client_s_dn always;
      return 200 '{"backend":"wronghost","client":"$ssl_client_s_dn"}';
    }
  }
}
